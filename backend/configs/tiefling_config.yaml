data:
    lexicon_fname: ./data/lexemes.csv
    phoneme_fname: ./data/phonemes.csv
    morpheme_fname: ./data/morphemes.csv
    adposition_fname: ./data/adpositions.csv
    sentences_fname: ./data/sentences.csv
params:
    # sov | svo | vso | vos | osv | ovs
    word_order: sov

    # pre | post | morpheme
    adpositions: post

    # none | low | high
    noun_class: none

    # none | low | high
    noun_case: none

    # no | yes
    verb_copula: no

    # none | low | high
    verb_tense: high

    # none | low | high
    verb_person: none

    # none | low | high
    verb_number: none

    # none | low | high
    verb_gender: none

    # none | low | high
    verb_aspect: high

    # none | low | high
    verb_mood: high

    # none | low | high
    verb_attitudinal: high

    # none | low | high
    verb_evidential: high

    # none | low | high
    derivational: high

    # none | low | high
    verb_valence: high

    min_syl_per_word: 1
    max_syl_per_word: 4
    min_syl_per_morpheme: 1
    max_syl_per_morpheme: 1

    # none | low | high
    pronouns: low

    # none | low | high
    onomatopoeia: none

    # none | low | med | high
    phon_cons_difficulty: med

    # none | low | med | high
    phon_cons_number: med

    # none | low | med | high
    phon_vowel_difficulty: low

    # none | low | med | high
    phon_vowel_number: med

    # none | low | high
    phon_syl_cons: none

    # none | low | high
    phon_tone: none

    # no | yes
    phon_gemination: yes

    # no | yes
    phon_vowel_length: yes

    # none | low | high
    phon_initial_clusters: none

    # none | low | high
    phon_final_clusters: none

    # none | low | high
    phon_diphthongs: none

    # none | low | high
    phon_non_egressive: no

